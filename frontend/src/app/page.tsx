'use client';

import React from 'react';
import OmniViseDashboard from '../components/dashboard/OmniViseDashboard';

export default function HomePage() {
  return <OmniViseDashboard defaultAuthenticated={false} />;
}
